export async function identifyContact(){
    try {
        // if contact is present return contact in prescribed format
            // If both email and phone belong to the same contact, return existing details
            // If they belong to different contacts, link them by keeping the older contact primary and making the newer one secondary

        // if contact is not present create new one
            // if incoming request has either email or phone then incoming service will create secondary contact and link it to primary contact
            // if incoming request doesn't have both email and phone then incoming service will create primary contact
    } catch (error) {
        
    }
}